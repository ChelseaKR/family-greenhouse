# Dependencies & lockfiles (npm owns the lock's formatting)
node_modules
package-lock.json

# Build outputs and generated artifacts
dist
build
coverage
.lighthouseci

# Compiled config artifacts (emitted from *.config.ts — never hand-edited)
frontend/vite.config.js
frontend/vite.config.d.ts

# Capacitor native shells — generated Xcode/Gradle projects managed by the
# Capacitor CLI and native tooling, not Prettier (reformatting them churns
# every `npx cap sync` / Android Studio / Xcode touch).
frontend/android
frontend/ios

# RAG corpus sources — hand-authored prose embedded verbatim into
# plant-care-corpus-embeddings.json. Prettier's markdown emphasis rewrite
# (*x* → _x_) would silently desync the committed embeddings and trip the
# corpus drift guard. Treat them as data, not formatted source.
backend/src/data/plant-care-corpus

# Vendored portfolio-standards — upstream content synced verbatim from
# ChelseaKR/portfolio-standards by Renovate (see renovate.json + the pinned
# docs/standards/.standards-version). Not authored here; reformatting it would
# diverge from upstream and churn the version-sync diff. Treat as vendored.
docs/standards
