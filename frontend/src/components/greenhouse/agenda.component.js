import React, { Component } from "react";
import { withRouter } from '../common/with-router';

class GreenhouseAgenda extends Component {

    render() {
        return (
            <div>

            </div>
        );
    }
}

export default withRouter(GreenhouseAgenda);